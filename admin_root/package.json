{
  "name": "mocks_booking_admin",
  "version": "1.0.0",
  "description": "Admin dashboard for mock booking system",
  "scripts": {
    "dev": "cd admin_frontend && npm run dev",
    "dev:api": "vercel dev",
    "build": "cd admin_frontend && npm install && npm run build",
    "deploy": "vercel --prod",
    "deploy:staging": "vercel",
    "test": "jest",
    "test:coverage": "jest --coverage"
  },
  "dependencies": {
    "axios": "^1.12.2",
    "cors": "^2.8.5",
    "dotenv": "^17.2.2",
    "express": "^5.1.0",
    "ioredis": "^5.8.1",
    "joi": "^18.0.1",
    "jsonwebtoken": "^9.0.2",
    "bcryptjs": "^2.4.3"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^24.5.2",
    "jest": "^30.1.3",
    "supertest": "^7.1.4"
  }
}