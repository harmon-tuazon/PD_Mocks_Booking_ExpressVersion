{
  "name": "mocks_booking_user",
  "version": "1.0.0",
  "description": "Student-facing booking application",
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "dev:api": "vercel dev",
    "build": "cd frontend && npm install && npm run build",
    "deploy": "vercel --prod",
    "deploy:staging": "vercel"
  },
  "dependencies": {
    "axios": "^1.12.2",
    "cors": "^2.8.5",
    "dotenv": "^17.2.2",
    "express": "^5.1.0",
    "ioredis": "^5.8.1",
    "joi": "^18.0.1"
  }
}
